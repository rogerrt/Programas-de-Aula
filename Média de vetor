#include<stdio.h>
#define tam 10

int main()
{
    int i;
    float vetor[tam], media=0;

    for ( i = 0; i < tam; i++)
    {
        scanf("%f", &vetor[i]);
        media += vetor[i];
    }
    media = media/10;
    printf("A media e: %f", media);
    return 0;
}